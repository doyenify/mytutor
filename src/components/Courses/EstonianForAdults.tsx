import React from 'react'
import { useTranslation } from 'react-i18next';

const EstonianForAdults = () => {
    const {t} = useTranslation();
  return (
    <>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    <div> {t('Estonian For Adult')}</div>
    </>
  )
}

export default EstonianForAdults